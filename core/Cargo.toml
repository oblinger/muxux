[package]
name = "muxux-core"
version = "0.1.0"
edition = "2021"
description = "MuxUX domain logic — layout, session, structure management"

[dependencies]
cmx-utils = { path = "../../cmx-utils" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
libc = "0.2"
